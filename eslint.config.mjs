// TODO(types): Re-enable strict `@typescript-eslint/no-explicit-any` as `error`
// once the current API and hook layers are fully typed.
export default [
  {
    ignores: [".next/**", "node_modules/**", "out/**", "build/**"],
  },
]
